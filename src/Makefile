
test:
	mkdir -p build
	cd build && cmake .. && cmake --build . --target MazeTests
	build/tests/MazeTests

install:
	mkdir -p build
	cd build && cmake .. && cmake --build . --target Maze

cave:
	mkdir -p build
	cd build && cmake .. && cmake --build . --target Cave

